{
  "name": "{{PROJECT_SLUG}}",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "prepare": "husky"
  },
  "devDependencies": {
    "@commitlint/cli": "^19.0.0",
    "@commitlint/config-conventional": "^19.0.0",
    "husky": "^9.0.0",
    "lint-staged": "^15.0.0"
  },
  "lint-staged": {
    "{{BACKEND_DIR}}/**/*.{ts,js}": ["eslint --fix --max-warnings=0"],
    "{{FRONTEND_DIR}}/**/*.{ts,tsx,js,jsx}": ["eslint --fix --max-warnings=0"]
  }

